#include <iostream>
#include <cmath>
using namespace std;

int main() {
    cout << sqrt(16) << endl;     // 4.0
    cout << pow(2, 3) << endl;    // 8.0
    cout << max(10, 20) << endl;  // 20
    cout << min(10, 20) << endl;  // 10

    return 0;
}