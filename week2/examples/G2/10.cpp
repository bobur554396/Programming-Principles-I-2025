#include <iostream>
#include <string>

using namespace std;

int main() {
    // 4. Conditional Statements

    /*
    if (condition) {
        // code to execute if condition is true
    }
    */
    int a;
    cin >> a;
    
    if(a > 0) {
        cout << "Positive" << endl;
    }


    return 0;
}