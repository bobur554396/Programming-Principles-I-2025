#include <iostream>

using namespace std;

int main() {

    /*
    - [ ] (++, --, +=, -=, *=, /=, %= )
    */
    int a = 3;
    a += 4; // a = a + 4;
    a++; // a += 1; // a = a + 1;
    a--; // a -= 1; // a = a - 1;

    cout << a << endl;


    return 0;
}

