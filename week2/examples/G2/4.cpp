#include <iostream>

using namespace std;

int main() {

    /*
    - [ ] (++, --, +=, -=, *=, /=, %= )
    */
    int a = 3;
    a += 4; // a = a + 4;
    a -= 2; // a = a - 2;
    a %= 3; // a = a % 3;

    cout << a << endl;


    return 0;
}

