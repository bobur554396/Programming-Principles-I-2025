#include <iostream>

using namespace std;

int main() {
    // - [x] (++, --, +=, -=, *=, /=, %= )

    int a = 2;
    int b = 3;
    // a += 1; // a = a + 1;
    // a *= 3; // a = a * 3;

    // a++; // a = a + 1; a += 1;
    a--; // a = a - 1; a -= 1;


    cout << a << endl;


    return 0;
}